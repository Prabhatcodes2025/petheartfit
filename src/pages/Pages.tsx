import { ArrowRight, Check, Search, Share2 } from 'lucide-react'
import { FormEvent, useMemo, useState } from 'react'
import { Link, useParams, useSearchParams } from 'react-router-dom'
import { MediaGallery } from '../components/Media'
import { Content, Breadcrumb } from '../components/Content'
import { FAQList } from '../components/FAQ'
import { PageHero, SectionTitle, whatsapp } from '../components/Layout'
import { LeadButton } from '../components/LeadPopup'
import { faqs, locations, posts, services, siteSettings, testimonials } from '../data'

export function About(){return <><PageHero eyebrow="Our point of view" title="Training should deepen trust—not demand compliance." text="PAWREXIO exists to bring clarity, compassion and professional structure to the moments that shape life with a pet."/><section className="section"><div className="container manifesto"><div><p className="eyebrow">Why PAWREXIO</p><h2>A premium standard measured in how the animal feels.</h2></div><div><p className="lead">We believe good behaviour work considers the whole picture: health, environment, emotion, reinforcement history and the people on the other end of the lead.</p><p>Our identity is built around a simple progression—train with clarity, earn trust through consistency, and transform the relationship through everyday practice.</p></div></div></section><section className="section values"><div className="container"><SectionTitle eyebrow="What guides the work" title="Principles before promises."/><div className="value-grid">{[['01','Welfare first','No result is worth compromising physical or emotional wellbeing.'],['02','Individual plans','Breed can inform us; the pet in front of us decides the pace.'],['03','Honest expectations','We avoid guarantees and explain what meaningful progress requires.'],['04','Lasting capability','Pet parents learn the mechanics so success is not trainer-dependent.']].map(x=><div key={x[0]}><span>{x[0]}</span><h3>{x[1]}</h3><p>{x[2]}</p></div>)}</div></div></section><section className="final-cta"><div className="container"><p className="eyebrow">Start the conversation</p><h2>Tell us what life with your pet feels like today.</h2><LeadButton>Book an assessment</LeadButton></div></section></>}

export function Locations(){return <><PageHero eyebrow="Service areas" title="Training where behaviour happens." text="Our location model supports home, neighbourhood and outdoor sessions where available. Active areas can be updated from the PAWREXIO admin."/><section className="section"><div className="container city-grid">{locations.map((l,i)=><Link to={`/locations/${l.slug}`} key={l.id}><img src={l.image} alt={`Pawrexio pet support in ${l.city}`} width="600" height="420" loading="lazy"/><span>0{i+1}</span><p>{l.state}</p><h2>{l.city}</h2><p>{l.intro.slice(0,190)}…</p><b>Explore this area <ArrowRight/></b></Link>)}</div></section></>}
export function LocationDetail(){const {slug}=useParams();const l=locations.find(x=>x.slug===slug);if(!l)return null;return <><PageHero eyebrow={`Professional pet training · ${l.state}`} title={l.heading||`Pet training in ${l.city}`} text={l.intro.slice(0,230)} image={l.image}/><Breadcrumb items={[{label:"Locations",to:"/locations"},{label:l.city}]}/><section className="section"><div className="container detail-grid"><div className="detail-main"><SectionTitle eyebrow={`Local support in ${l.city}`} title="Built for your pet’s everyday environment."/><h2>Services available by enquiry</h2><div className="benefit-list">{services.slice(0,6).map(s=><Link to={`/services/${s.slug}`} key={s.id}><Check/><span>{s.title}</span><ArrowRight/></Link>)}</div><Content blocks={l.blocks?.slice(1)}/><h2>Local questions</h2><FAQList items={[[`Can sessions take place at home in ${l.city}?`,`Home visits can be requested in active coverage zones within ${l.city}. Availability is confirmed after reviewing your address and training needs.`],[`Which package is best for city dogs?`,`The right package depends on age, current skills and goals. A free consultation helps us recommend the most useful starting point.`],['Can training happen outdoors?','Yes, where safe and appropriate. Skills can progress into real environments after clear foundations are established.']]}/></div><aside className="detail-aside"><p className="eyebrow">Check availability</p><p>Coverage varies by neighbourhood and trainer schedule. Send your postcode or locality for a precise answer.</p><a className="button button-outline" href={`tel:${siteSettings.phone}`}>Call {siteSettings.phone}</a><LeadButton location={l.city}>Check {l.city} availability</LeadButton><a className="button button-outline" href={whatsapp(`pet training in ${l.city}`)} target="_blank" rel="noreferrer">WhatsApp</a></aside></div></section><section className="section container"><h2>Other locations</h2><div className="location-mini-grid">{locations.filter(x=>x.id!==l.id).map(x=><Link key={x.id} to={`/locations/${x.slug}`}>{x.city}</Link>)}</div></section></>}

export function Gallery(){return <><PageHero eyebrow="Real Pawrexio sessions" title="Focus, connection, progress." text="Real moments from our training and care archive." image="/assets/real/attention.webp"/><section className="section"><div className="container"><MediaGallery/></div></section></>}

export function Testimonials(){return <><PageHero eyebrow="Client feedback" title="Trust is earned in the details." text="Pet-parent experiences adapted from the approved reference content supplied for this rebuild."/><section className="section"><div className="container testimonial-grid">{testimonials.map(t=><blockquote className="testimonial-card" key={t.id}><span>“</span><p>{t.quote}</p><footer><b>{t.name}</b><small>{[t.pet,t.location].filter(Boolean).join(' · ')}</small></footer></blockquote>)}</div></section></>}

export function Blog(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const filtered=useMemo(()=>posts.filter(p=>(cat==='All'||p.category===cat)&&`${p.title} ${p.excerpt}`.toLowerCase().includes(q.toLowerCase())),[q,cat]);const cats=['All',...new Set(posts.map(p=>p.category))];return <><PageHero eyebrow="The training journal" title="Better questions make better pet parents." text="Clear, humane guidance for training, behaviour and the everyday choices that shape your pet’s life."/><section className="section"><div className="container"><div className="blog-tools"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the journal"/></label><div>{cats.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div></div><div className="blog-grid">{filtered.map((p,i)=><Link className={i===0?'featured':''} to={`/blog/${p.slug}`} key={p.id}><img src={p.image} alt=""/><div><p>{p.category} · {p.readTime}</p><h2>{p.title}</h2><span>{p.excerpt}</span><b>Read article <ArrowRight/></b></div></Link>)}</div></div></section></>}
export function BlogDetail(){const {slug}=useParams();const p=posts.find(x=>x.slug===slug);if(!p)return null;return <><PageHero eyebrow={`${p.category} · ${p.readTime}`} title={p.title} text={p.excerpt} image={p.image}/><article className="article container"><div className="article-meta"><span>Published {p.date}</span><button onClick={()=>navigator.share?.({title:p.title,url:location.href})}><Share2/> Share</button></div>{p.content.map(x=><p key={x}>{x}</p>)}<blockquote>Progress comes from making the desired choice easier to understand and worth repeating.</blockquote><h2>When to get professional help</h2><p>If behaviour is escalating, affecting safety, or causing distress for your pet or household, a qualified professional can help you assess context and build a realistic plan.</p><LeadButton service={p.category}>Discuss your pet</LeadButton></article></>}

export function FAQPage(){return <><PageHero eyebrow="Helpful answers" title="Clarity before you begin." text="Straightforward answers about our approach, session structure and what meaningful training asks of pet parents."/><section className="section"><div className="container narrow"><FAQList items={faqs}/></div></section></>}

type FormState={fullName:string;phone:string;email:string;city:string;petType:string;petName:string;breed:string;petAge:string;service:string;concern:string;preferredDate:string;preferredTime:string;message:string;website:string}
const initial:FormState={fullName:'',phone:'',email:'',city:'',petType:'Dog',petName:'',breed:'',petAge:'',service:'',concern:'',preferredDate:'',preferredTime:'',message:'',website:''}
export function Booking({contact=false}:{contact?:boolean}){const [params]=useSearchParams();const [form,setForm]=useState({...initial,service:params.get('service')||params.get('program')||'',city:params.get('city')||''});const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setStatus('loading');setError('');try{const res=await fetch('/api/enquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,source:'website',originatingPage:location.pathname})});const body=await res.json();if(!res.ok)throw new Error(body.message||'Unable to send enquiry');setStatus('success');setForm(initial)}catch(e){setError(e instanceof Error?e.message:'Something went wrong');setStatus('error')}}return <><PageHero eyebrow={contact?'Contact PAWREXIO':'Book a session'} title={contact?'Let’s talk about your pet.':'Begin with the full picture.'} text="Share what you are seeing, where you are located and what you would like life together to feel like. We’ll recommend a considered next step." image="/assets/real/puppy-focus.webp"/><section className="section"><div className="container form-layout"><div><SectionTitle eyebrow="Your enquiry" title="Tell us what matters."/><p>Required fields are marked with an asterisk. Your details are used only to respond to this enquiry.</p><div className="contact-note"><b>Prefer email?</b><a href={`mailto:${siteSettings.email}`}>{siteSettings.email}</a><a href={`tel:${siteSettings.phone}`}>{siteSettings.phone}</a></div></div>{status==='success'?<div className="success-state"><Check/><h2>Thank you. Your enquiry is in.</h2><p>We have saved your details and the PAWREXIO team will respond as soon as possible.</p><button className="button button-outline" onClick={()=>setStatus('idle')}>Send another</button></div>:<form className="booking-form" onSubmit={submit}><div className="field-row"><Field label="Full name" required value={form.fullName} set={v=>setForm({...form,fullName:v})}/><Field label="Phone" required type="tel" value={form.phone} set={v=>setForm({...form,phone:v})}/></div><div className="field-row"><Field label="Email" type="email" value={form.email} set={v=>setForm({...form,email:v})}/><Field label="City / locality" required value={form.city} set={v=>setForm({...form,city:v})}/></div><div className="field-row"><label>Pet type<select value={form.petType} onChange={e=>setForm({...form,petType:e.target.value})}><option>Dog</option><option>Cat</option><option>Other</option></select></label><Field label="Pet name" value={form.petName} set={v=>setForm({...form,petName:v})}/></div><div className="field-row"><Field label="Breed" value={form.breed} set={v=>setForm({...form,breed:v})}/><Field label="Pet age" value={form.petAge} set={v=>setForm({...form,petAge:v})}/></div><label>Service required<select required value={form.service} onChange={e=>setForm({...form,service:e.target.value})}><option value="">Choose a service</option>{services.map(s=><option value={s.slug} key={s.id}>{s.title}</option>)}<option value="not-sure">Not sure yet</option></select></label><label>Behaviour concern<textarea required value={form.concern} onChange={e=>setForm({...form,concern:e.target.value})} placeholder="What would you like help with?"/></label><div className="field-row"><Field label="Preferred date" type="date" value={form.preferredDate} set={v=>setForm({...form,preferredDate:v})}/><label>Preferred time<select value={form.preferredTime} onChange={e=>setForm({...form,preferredTime:e.target.value})}><option value="">Flexible</option><option>Morning</option><option>Afternoon</option><option>Evening</option></select></label></div><label>Anything else?<textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label><input className="honeypot" tabIndex={-1} autoComplete="off" value={form.website} onChange={e=>setForm({...form,website:e.target.value})}/>{status==='error'&&<p className="form-error" role="alert">{error}</p>}<button className="button" disabled={status==='loading'}>{status==='loading'?'Sending…':'Send my enquiry'} <ArrowRight/></button><small>By submitting, you agree to our <Link to="/privacy">privacy policy</Link>.</small></form>}</div></section></>}
function Field({label,value,set,type='text',required=false}:{label:string;value:string;set:(v:string)=>void;type?:string;required?:boolean}){return <label>{label}{required?' *':''}<input type={type} required={required} value={value} onChange={e=>set(e.target.value)} min={type==='date'?new Date().toISOString().slice(0,10):undefined}/></label>}

export function Legal({type}:{type:'privacy'|'terms'}){const privacy=type==='privacy';return <><PageHero eyebrow="PAWREXIO policies" title={privacy?'Privacy policy':'Terms & conditions'} text={`Last updated 20 August 2026. This ${privacy?'policy explains how enquiry and service information is handled':'document sets out the basis on which PAWREXIO enquiries and services are provided'}.`}/><article className="article container"><h2>{privacy?'Information we collect':'Service information'}</h2><p>{privacy?'We collect information you choose to provide through enquiry forms, including contact details, pet information and your preferred service. Technical security logs may also be retained for a limited period.':'Program descriptions and indicative durations are informational. A final plan, schedule, price and scope are agreed directly after assessment and subject to availability.'}</p><h2>{privacy?'How information is used':'Appointments and payment'}</h2><p>{privacy?'Information is used to respond to enquiries, manage bookings, deliver services and protect our systems. We do not sell personal information. Service providers are used only where necessary and under appropriate safeguards.':'Confirmed bookings, cancellation terms and payment milestones will be provided in writing before service begins. Website submission alone does not create a confirmed appointment.'}</p><h2>{privacy?'Retention and your choices':'Animal welfare and safety'}</h2><p>{privacy?`Records are retained only as long as needed for the purposes above or legal obligations. You may request access, correction or deletion by emailing ${siteSettings.email}.`:'Pet parents must disclose relevant medical and behavioural history. PAWREXIO may pause or adapt a session when safety or welfare requires it, and may recommend veterinary or specialist support.'}</p><h2>Contact</h2><p>Questions can be sent to {siteSettings.email}. Registered address and formal legal entity details should be added before public launch.</p></article></>}

export function NotFound(){return <section className="not-found"><span>404</span><p className="eyebrow">This trail ends here</p><h1>Let’s find the way back.</h1><Link className="button" to="/">Return home</Link></section>}
